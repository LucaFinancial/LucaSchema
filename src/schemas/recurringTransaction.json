{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/LucaFinancial/LucaSchema/main/src/schemas/recurringTransaction.json",
  "title": "Recurring Transaction",
  "description": "Defines recurring financial transactions within the application, including their scheduling, frequency, interval, and optional constraints like total occurrences or an end date.",
  "type": "object",
  "allOf": [
    {
      "$ref": "./common.json"
    }
  ],
  "properties": {
    "postings": {
      "type": "array",
      "title": "Postings",
      "minItems": 2,
      "items": {
        "$ref": "./posting.json"
      },
      "description": "Array of posting templates for this recurring transaction. Must contain at least 2 postings and the sum of all posting amounts must equal zero."
    },
    "categoryId": {
      "type": ["string", "null"],
      "title": "Category ID",
      "format": "uuid",
      "description": "Category identifier for organizing the transaction. Can be null if not categorized. This is a decorative field that may be derived from the primary debit or credit account."
    },
    "description": {
      "type": "string",
      "title": "Description",
      "maxLength": 500,
      "description": "A brief description of the recurring transaction."
    },
    "frequency": {
      "type": "string",
      "title": "Transaction Frequency",
      "enum": ["DAY", "WEEK", "MONTH", "YEAR"],
      "description": "Defines the base unit of time for the repetition."
    },
    "interval": {
      "type": "integer",
      "title": "Frequency Interval",
      "minimum": 1,
      "description": "Specifies the number of frequency units between each occurrence (e.g., every 2 weeks)."
    },
    "occurrences": {
      "type": ["integer", "null"],
      "title": "Total Occurrences",
      "minimum": 1,
      "description": "The total number of times the transaction should occur. Can be null if not specified."
    },
    "startOn": {
      "type": "string",
      "title": "Start Date",
      "format": "date",
      "description": "The date on which the recurring transaction series should begin."
    },
    "endOn": {
      "type": ["string", "null"],
      "title": "End Date",
      "format": "date",
      "description": "The date on which the recurring transaction series should end. Can be null."
    },
    "recurringTransactionState": {
      "type": "string",
      "title": "Recurring Transaction State",
      "enum": ["ACTIVE", "PAUSED", "COMPLETED", "CANCELLED"],
      "description": "Current state of the recurring transaction series."
    }
  },
  "required": [
    "postings",
    "description",
    "frequency",
    "interval",
    "occurrences",
    "startOn",
    "endOn",
    "recurringTransactionState"
  ]
}
